##### Matlab and R don't match ): 

# a = c(2, 1, 3, 4, 1)
# b = c(5, 2, 1, 0, 5)
# c2 = c(1, 3, 3, 9, 5)
# wilcox.test(a, b, paired = TRUE, correct = FALSE, exact = FALSE)

# test = wilcox.test(c1, c2, alternative = "two.sided", exact = FALSE, correct = FALSE)
# test
# 
# tail(df$x2)